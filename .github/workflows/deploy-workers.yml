name: Deploy Workers

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "packages/workers/**"
      - ".github/workflows/deploy-workers.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workers
        run: pnpm --filter workers... run build --if-present

      - name: Deploy workers
        run: echo "TODO: add deploy commands (e.g. fly deploy, docker push, etc.)"
